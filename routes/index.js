const express = require('express');
const router = express.Router();
const bodyParser = require('body-parser');
//const models = require('../src/models/errorModel');


//Error Post
// router.post('/error/list', bodyParser.json());

module.exports = router;
